{
  "name": "conjure-lite.example",
  "private": true,
  "dependencies": {
    "conjure-lite": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.16.0",
    "dprint": "^0.50.1",
    "typescript": "^5.8.3",
    "zod": "^3.25.75"
  },
  "scripts": {
    "build": "tsc --build",
    "codegen": "rm -rf src/generated lib tsconfig.tsbuildinfo && node ../../conjure-lite/dist/conjure-lite.mjs generate --ir ir.json --outDir src/generated --header '/* sample header */' --zod && dprint fmt 'src/generated/**/*'",
    "test": "vitest run"
  },
  "type": "module"
}